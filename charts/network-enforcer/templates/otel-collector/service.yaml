{{- if eq .Values.telemetry.collectorStrategy "default" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "network-enforcer.fullname" . }}-otel-collector
  labels:
  {{- include "network-enforcer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "network-enforcer.fullname" . }}-otel-collector
    app.kubernetes.io/component: otel-collector
  ports:
  - name: otlp-grpc
    port: 4317
    targetPort: otlp-grpc
    protocol: TCP
  - name: prometheus
    port: 9090
    targetPort: prometheus
    protocol: TCP
{{- end }}