{{- if eq .Values.telemetry.collectorStrategy "default" }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "runtime-enforcer.fullname" . }}-otel-collector
  labels:
  {{- include "runtime-enforcer.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "runtime-enforcer.fullname" . }}-otel-collector
      app.kubernetes.io/component: otel-collector
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "runtime-enforcer.fullname" . }}-otel-collector
        app.kubernetes.io/component: otel-collector
    spec:
      {{- if .Values.priorityClass.name }}
      priorityClassName: {{ .Values.priorityClass.name }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
      {{- include "imagePullSecrets" .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      containers:
      - name: otel-collector
        image: {{ .Values.telemetry.defaultCollector.image.repository }}:{{ .Values.telemetry.defaultCollector.image.tag }}
        imagePullPolicy: {{ .Values.telemetry.defaultCollector.image.pullPolicy }}
        command:
        - /otelcol-contrib
        - --config=/etc/otel/config.yaml
        ports:
        - name: otlp-grpc
          containerPort: 4317
          protocol: TCP
        - name: prometheus
          containerPort: 9090
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /
            port: 13133
        readinessProbe:
          httpGet:
            path: /
            port: 13133
        resources: {{- toYaml .Values.telemetry.defaultCollector.resources | nindent 10 }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 10001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - name: otel-collector-config
          mountPath: /etc/otel
          readOnly: true
        - name: otel-collector-tls
          mountPath: {{ include "runtime-enforcer.otelCollector.certDir" . }}
          readOnly: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - name: otel-collector-config
        configMap:
          name: {{ include "runtime-enforcer.fullname" . }}-otel-collector
      - name: otel-collector-tls
        csi:
          driver: "csi.cert-manager.io"
          readOnly: true
          volumeAttributes:
            csi.cert-manager.io/issuer-name: {{ include "runtime-enforcer.caIssuerName" . }}
            csi.cert-manager.io/issuer-kind: Issuer
            csi.cert-manager.io/dns-names: >-
              {{ include "runtime-enforcer.fullname" . }}-otel-collector,
              {{ include "runtime-enforcer.fullname" . }}-otel-collector.{{ .Release.Namespace }},
              {{ include "runtime-enforcer.fullname" . }}-otel-collector.{{ .Release.Namespace }}.svc,
              {{ include "runtime-enforcer.fullname" . }}-otel-collector.{{ .Release.Namespace }}.svc.cluster.local
{{- end }}
