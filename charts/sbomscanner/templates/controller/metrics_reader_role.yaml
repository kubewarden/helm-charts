apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "sbomscanner.fullname" . }}-controller-metrics-reader
  labels:
    {{ include "sbomscanner.labels" .| nindent 4 }}
    app.kubernetes.io/component: controller
rules:
- nonResourceURLs:
  - "/metrics"
  verbs:
  - get
