policies:
  - name: Update Helm chart dependencies and Hauler manifest
    config:
      - ./updatecli.d/patch-kubewarden-defaults.yaml
    values:
      - ./values.yaml
      - ./release-values.yaml
