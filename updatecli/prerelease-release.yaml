policies:
  - name: Update Helm chart dependencies and Hauler manifest
    config:
      - ./updatecli.d/prerelease-kubewarden-update.yaml
    values:
      - ./values.yaml
      - ./release-values.yaml
