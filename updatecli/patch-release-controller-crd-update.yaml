policies:
  - name: Update Helm chart dependencies and Hauler manifest
    config:
      - ./updatecli.d/patch-kubewarden-controller-with-crds-update.yaml
    values:
      - ./values.yaml
      - ./release-values.yaml
